# 贡献指南

感谢你愿意改进这套技能。提交前请先跑一遍校验脚本。

## 环境

只需要 Python 3.9 及以上，无第三方依赖：

```bash
python scripts/build_dist.py --check
```

## 可以贡献什么

- **新增框架**：从书里提取一张框架卡，编译成新的原子技能。
- **修订判断规则**：某条「如果……则……」在实际使用中不成立，改成更可判定的表述。
- **补反例与测试案例**：反例比正例更能约束 Agent 的行为。
- **改进路由**：`problem-analysis-framework` 的选型对照有误判时修正。

## 技能文件约定

技能放在仓库根目录 `skills/` 下。每个技能是一个目录，必须包含 `SKILL.md`：

```text
<技能名>/
├── SKILL.md          # 必需
├── references/       # 可选，按需加载的长文档
├── scripts/          # 可选，确定性脚本
└── assets/           # 可选，输出用素材
```

`SKILL.md` 的 YAML frontmatter 只允许两个字段，且都必须存在：

```yaml
---
name: five-whys
description: 一句话说清这个技能做什么，以及什么情况下该用。
---
```

约束：

1. `name` 必须是连字符小写（hyphen-case），且与目录名完全一致。
2. `description` 写清「做什么 + 何时用」，不要包含尖括号 `<` `>`。
3. 正文用第三人称、命令式表述，不要写「你可以……」。
4. 正文控制在 5000 字以内，长内容放 `references/`，避免与 `SKILL.md` 重复。
5. 所有示例必须是重述，禁止摘抄书籍原文。

## 技能正文的推荐结构

按此顺序组织，便于 Agent 顺序读取：

一句话定义 → 何时使用 → 何时不使用 → 输入 → 输出 → 执行步骤 →
判断规则 → 提问清单 → 常用工具 → 常见错误 → 质量检查 → 正例 → 反例 →
测试案例 → 来源

## 提交步骤

1. 新建分支：`git checkout -b add-<技能名>`。
2. 修改或新增技能文件。
3. 跑校验：`python scripts/build_dist.py --check`。
4. 如涉及打包结构变更，本地生成一次产物确认无误：`python scripts/build_dist.py`。
5. 提交 PR，说明动机和验证方式。

提交信息建议格式：`feat(five-whys): 补充多分支转为因果树的判断规则`。

## 命名与代码规范

`scripts/` 下的脚本遵循以下约定：

- 禁止 `a`、`x1`、`tmp` 这类无信息量的命名，布尔值用肯定状态（`is_ready`，不用 `not_disabled`）。
- 函数名表达「动作 + 结果」，如 `collect_skill_problems`。
- 单个函数有效代码不超过 30 行，嵌套不超过 3 层，边界条件提前返回。
- 调用多参数函数时显式写明参数名。
- 注释只解释「为什么」，不复述「做什么」。

## 许可证

提交贡献即表示同意以本仓库的 [MIT 许可证](LICENSE) 发布。
